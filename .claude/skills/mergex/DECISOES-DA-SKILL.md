# Decisões tomadas na construção da skill mergex

Ambiguidades do prompt de especificação resolvidas pela opção mais conservadora, uma linha de contexto cada.

| # | Ambiguidade | Decisão tomada | Motivo |
|---|---|---|---|
| DM-01 | O prompt descreve nove blocos (E0–E8 mais o comando manual), mas nunca dá um número à etapa de revisão, e a árvore pede `09-revisao.md` | A revisão é a **etapa E9**, nomeada assim em toda a skill | A árvore já numera o reference como `09`; deixar a etapa sem número tornaria as tabelas e as regras ambíguas |
| DM-02 | Colisão de nomes: os estágios da runx são E1–E5 e as etapas da mergex são E0–E9 — "E3" significa duas coisas | Toda menção diz de qual skill é o estágio ("E3 da runx", "E3 da mergex"), e o `integracao/runx.md` abre com o aviso da colisão | Um agente lendo "aciona no E3" sem qualificação erraria o ponto de acionamento em metade dos casos |
| DM-03 | O prompt não define onde ficam `PR.md` e `ATENCAO.md` — só cita `ENTREGA.md` e `QA-PACOTE.md` em `docs/entregas/<trabalho_id>/` | Os quatro arquivos ficam na mesma pasta `docs/entregas/<trabalho_id>/` | É a única pasta de entrega que o prompt define; espalhar artefatos da mesma entrega quebraria os links relativos entre eles |
| DM-04 | O prompt manda gravar `PR.md` quando a ferramenta de PR está ausente, mas não diz se ele é gravado quando o PR abre normalmente | `PR.md` é gravado **sempre**, no E4, antes do E7 | O arquivo é o registro da entrega; o PR no serviço é a cópia dele. Gravar só na falha deixaria a entrega sem registro no caminho feliz |
| DM-05 | O prompt lista dez itens do portão em prosa corrida, sem identificadores | Numerados **V1 a V10**, e todos aparecem sempre na tabela de saída, inclusive os `n/a` | Sem identificador não há como a saída dizer "V2 falhou"; marcar `n/a` em vez de omitir distingue "não se aplica" de "esqueceram de verificar" |
| DM-06 | O portão barra por "QA.md do runx reprovado", mas o prompt não diz o que barra num trabalho da sprintx, que não tem QA | V5 (QA) é `n/a` na sprintx, e foi criada a **V6 — auditoria da sprintx reprovada**, derivada de "F5 auditoria reprovada faz E2 barrar" | A regra existe no prompt (seção de integração), mas não estava na lista das dez; sem V6 a auditoria reprovada não teria onde barrar |
| DM-07 | Os critérios das três faixas são listados em prosa, sem identificadores | Numerados **O1–O8, L1–L3, D1–D3**, e toda justificativa tem que nomear pelo menos um | A regra 8 exige classificação derivada de evidência; sem identificador, "por quê" vira prosa e a auditoria da classificação fica impossível |
| DM-08 | O prompt não diz o que fazer com arquivo do diff que não bate em nenhum critério de nenhuma faixa | Vai para **OLHO OBRIGATÓRIO por padrão**, com a razão declarada ("sem evidência de cobertura; classificado no padrão") | "Na dúvida, sobe para a mais rigorosa" é a regra dura; ausência de evidência é ausência de prova, não prova de segurança |
| DM-09 | O prompt não diz como classificar arquivo **removido** (`D` no diff) | Classificado pelo que ele era; **teste removido nunca é DISPENSÁVEL** — é perda de cobertura, logo OLHO OBRIGATÓRIO | D1 fala de teste que "só acrescenta caso"; um teste apagado é o oposto disso, e é exatamente onde um bug entra sem ninguém ver |
| DM-10 | O critério D3 dispensa "arquivo gerado automaticamente, quando declarado como tal", mas migrações de ORM também são geradas | **Migração de banco é sempre O3**, mesmo gerada; D3 só vale para arquivo gerado que *reflete* algo já revisado e não tem efeito próprio | O prompt manda migração de banco ser OLHO OBRIGATÓRIO ("qualquer uma", regra 9); a exceção de arquivo gerado não pode engolir uma regra inviolável |
| DM-11 | O prompt não define o que conta como "declarado como tal" para arquivo gerado | Declaração no `CONVENCOES.md` da stackx, cabeçalho "generated by" no próprio arquivo, ou `linguist-generated` no `.gitattributes`. **Sem declaração, não é tratado como gerado** | Assumir que algo é gerado porque parece gerado é classificação por sensação, e viola a regra 8 |
| DM-12 | O prompt define o formato da mensagem de commit, mas não diz o que fazer quando a task não tem escopo evidente | Omitir os parênteses: `fix: <título>` | É a forma canônica quando não há escopo; inventar um escopo seria gerar conteúdo novo (regra 7) |
| DM-13 | O prompt manda "nunca commitar arquivo fora da lista declarada", mas não diz o que fazer com o arquivo que ficou de fora | Não entra no commit, não é apagado, fica na árvore, e é registrado em `desvios` no `ENTREGA.md`; a V9 do portão barra depois, nomeando-o | Apagar seria descartar trabalho de alguém (proibido); commitar violaria a regra 4; barrar depois devolve a decisão para a pessoa |
| DM-14 | O prompt não diz como adicionar arquivos ao commit | Por caminho explícito, nunca `git add .`, `-A` ou `-u` | Os atalhos arrastam o que não foi declarado, o que violaria a regra 4 silenciosamente |
| DM-15 | O prompt manda varrer segredo mas não define os sinais nem o que é falso positivo | Tabela de quatro categorias (chave de API, credencial, chave privada, dado real de cliente) com sinais concretos; placeholder óbvio não é segredo; **na dúvida, trata como segredo** | Sem critério concreto a varredura vira sensação; a assimetria (na dúvida, barra) é a única segura para um falso negativo que vaza credencial |
| DM-16 | O prompt não diz se o valor do segredo encontrado aparece na mensagem de aviso | **Sempre mascarado** (`sk-...4f2a`), nunca ecoado em saída, log ou arquivo | Ecoar o segredo no relatório o espalharia para mais um lugar, que é o oposto do objetivo da verificação |
| DM-17 | O prompt não define a estratégia de detecção de "commits no remoto que a branch local não tem" | `git fetch` seguido de `git rev-list --count HEAD..origin/<branch>`; contagem maior que zero para e avisa | É a checagem que não altera nada da árvore; qualquer forma de reconciliar violaria "para e avisa em vez de reconciliar" |
| DM-18 | O prompt manda o E9 analisar conflitos, mas analisar exigiria tentar o merge | `git merge-tree` sobre a base de fusão, que calcula o conflito **sem tocar na árvore de trabalho e sem criar commit** | Fazer um merge de verdade "só para ver" deixaria estado sujo e poderia disparar um merge não confirmado |
| DM-19 | O prompt dá seis fatores para ordenar por impacto no E9, mas não a precedência entre eles | Ordem lexicográfica declarada: raio → arquivos em olho obrigatório → sobreposição → conflito → quantidade de arquivos → número do PR (desempate estável) | Sem precedência a ordenação não é reproduzível; o número do PR ao fim garante ordem estável entre PRs equivalentes |
| DM-20 | O prompt manda ordenar do menor para o maior impacto, mas não diz onde ficam os PRs inelegíveis (rascunho, CI vermelha) | Aparecem na lista, **no fim**, marcados com o motivo, e não entram na condução | Omiti-los esconderia trabalho aberto; oferecê-los violaria as recusas duras |
| DM-21 | O prompt não diz o que fazer quando `mergeable` do PR é desconhecido | Verifica localmente com `merge-tree`; se não for possível, marca "conflito não verificado" e **trata como conflito** | Conservador: tratar conflito desconhecido como ausente poderia oferecer para merge algo que quebra |
| DM-22 | O prompt define `pr_estado` com quatro valores mas não diz quando cada um é gravado, nem o valor antes de o PR existir | `rascunho` na abertura normal, `aberto` quando o QA já aprovou, `merged`/`fechado` atualizados depois; `null` enquanto não há PR | O contrato proíbe omitir chave; `null` é o valor de "não se aplica ainda" |
| DM-23 | O prompt não diz qual `expx_tool` o `ENTREGA.md` declara, já que a mergex não está no enum do schema v1 | `expx_tool` fica com a skill de **origem** (`sprintx`/`runx`) e foi acrescentado `entregue_por: mergex` | Mudar o enum de um contrato compartilhado quebraria o painel que lê as três skills; o campo novo é aditivo |
| DM-24 | O prompt não define os enums de `estado` e `portao` do `kind: entrega` | `estado`: `aberto`/`entregue`/`bloqueado`; `portao`: `pronto`/`bloqueado`/`null` | Derivados diretamente dos estados que o prompt descreve em prosa (E0 abre, E2 barra, E8 fecha) |
| DM-25 | O prompt não diz se o `ENTREGA.md` de um trabalho barrado no portão fica `bloqueado` ou `entregue` | `estado: bloqueado` e `portao: bloqueado`; o resto fica `null`/`false` | O painel precisa distinguir o que aguarda revisão do que travou; marcar como entregue algo barrado seria maquiar |
| DM-26 | O E0 exige árvore limpa, mas o prompt não diz se arquivo apenas **não rastreado** (`??`) conta | Conta. Qualquer linha de `git status --porcelain` para o E0 | Pode ser trabalho de alguém; a skill não julga o conteúdo do que não é dela |
| DM-27 | O prompt manda o E6 rodar "depois de E2 aprovar e do pacote de QA existir", mas não diz o que fazer sem remoto configurado | `push_feito: false`, informa que o trabalho está commitado localmente, e segue para o E8 sem erro | Mesma lógica da regra 12 (ferramenta ausente não é erro): a entrega local continua válida |
| DM-28 | O prompt manda abrir o PR "como rascunho quando o pacote de QA ainda não foi aprovado", mas o fluxo normal entrega antes do QA rodar | **Rascunho é o padrão**; pronto para revisão só com `VEREDITO: APROVADO` no `QA.md` | É o estado honesto no fluxo normal, e o E9 nunca faz merge de rascunho — o que fecha o ciclo |
| DM-29 | O prompt não diz se a mergex adiciona revisor, etiqueta ou responsável ao abrir o PR | Não adiciona nada por conta própria; só o que o `CONVENCOES.md` da stackx declarar | Escolher destinatário sem artefato que o sustente seria gerar conteúdo novo (regra 7) |
| DM-30 | O prompt manda a descrição do PR "caber em uma tela", o que conflita com "lista de arquivos completa" num diff grande | A lista de arquivos e a seção "onde eu quero seu olho" **nunca são cortadas**; corta-se prosa. Proibido resumir com "e mais N arquivos" | As duas seções são a razão de a skill existir (perceber sobreposição e saber onde olhar); o arquivo omitido seria justamente o que colide |
| DM-31 | O prompt não diz onde ancorar `docs/entregas/` em monorepo | Raiz do repositório mais próxima (a que contém `.git/`); sem `.git` em nenhum ancestral, a raiz do diretório de trabalho; reaproveitar `docs/` existente | Mesma regra que a sprintx e a runx já usam para `docs/`, mantendo as três árvores no mesmo lugar |
| DM-32 | O prompt não diz o que o E5 faz quando não existe roteiro de teste manual (trabalho da sprintx, que não tem `QA.md`) | Deriva os casos dos `criterio_aceite` das tasks, traduzidos para linguagem de tela, e registra o aviso de que o roteiro original não existia | Traduzir critério existente é costura (permitida); inventar caso de teste seria conteúdo novo (regra 7) |
| DM-33 | O prompt manda o pacote de QA apontar o relatório de uso da runx, que ainda não existe quando a mergex entrega | Aponta o caminho onde ele vai ficar e **diz explicitamente que ainda não existe** | O E5 da runx roda depois da entrega aceita (regra 19); apontar para um arquivo inexistente sem avisar confundiria o QA |
| DM-34 | O prompt não diz se a mergex pode escrever no `ORQUESTRADOR.md`, que é artefato das irmãs | Escreve **uma única linha**, registrando a branch, e nada mais | O prompt manda registrar a branch ali; limitar a escrita a essa linha preserva a propriedade do artefato pela skill de origem |
| DM-35 | Idioma dos arquivos da skill | Todo o conteúdo em pt-BR, inclusive a `description` do frontmatter | Prompt, marca, ecossistema e usuário são pt-BR, como na sprintx e na runx |
| DM-36 | Marcadores de preenchimento nos templates | Padrão `{{assim}}` em todos os assets, com nota no topo mandando substituir tudo e apagar o cabeçalho | Mesmo padrão da sprintx (DS-13), fácil de grepar por vazamento |
| DM-37 | O prompt manda a `legadox` alimentar o portão e a classificação, mas ela não está instalada nem tem formato de artefato publicado | Os references consomem os artefatos por **descrição funcional** (raio, zonas, caracterização, reversão), e o `patch-legadox.md` é quem define o formato estável (`FAIXA: ALTO`, tabela de zonas com caminhos) | Sem o patch aplicado, a mergex trata tudo como fonte ausente e segue; com ele, casa caminho de arquivo com zona de risco |
| DM-38 | O prompt não define o que é `PROPOSTA` no `CONVENCOES.md` além de "não governa" | Três estados explícitos: `OBSERVADO` governa, `PROPOSTA` vira aviso, `NAO DETERMINADO` deixa a mergex usar o próprio padrão | Sem os três estados nomeados, "não governa" não teria como ser verificado por outra skill |
| DM-39 | O prompt não diz o que fazer quando o `CONVENCOES.md` declara uma convenção que o repositório não pratica | Segue o `CONVENCOES.md` (é a decisão declarada do time) e registra a divergência como **aviso** | Convenção declarada vence detecção; a divergência é informação útil, mas nunca barra — o portão barra por trabalho incompleto, não por estilo |
| DM-40 | O prompt exige a description de `mergex-revisar` "estrita e não convidativa", sem dizer como impedir o disparo automático na prática | Description escrita em negativa explícita (não use por iniciativa própria, não encadeie, não ofereça ao fim de um trabalho) e reforçada no corpo do comando, no `09-revisao.md`, no `08-registro.md`, no `AGENTS.md` e nos quatro patches | Uma description apenas "estrita" ainda dispararia por similaridade semântica; a negativa explícita em todos os pontos de contato é o que efetivamente impede o encadeamento |

## Verificação da PARTE 3 — o que ficou registrado

| # | Achado durante a verificação | Resolução |
|---|---|---|
| DM-41 | O item 22 (grep por texto de exemplo vazado) acusa `{{...}}` em dois references e o caso fiscal em sete arquivos de instrução | **Não é vazamento.** Os `{{...}}` são a skill instruindo o agente a *detectar* marcador não substituído (V3 do portão e checklist do E4); o caso fiscal aparece como exemplo didático em prosa instrucional, mesmo recurso que a sprintx e a runx usam. Nenhum marcador não substituído sobrou em arquivo de instrução |
| DM-42 | O item 1 da verificação acusou um "arquivo faltando" na primeira execução | Artefato do laço do shell (linha em branco da lista), não arquivo real. Todos os 35 arquivos da árvore existem e nenhum está vazio |

## Decisões da implementação de hooks e agentes

| # | Ambiguidade | Decisão tomada | Motivo |
|---|---|---|---|
| DM-43 | O contrato manda o hook "sempre gravar no rastro, inclusive quando permite", mas o vocabulário de `evento` não tem termo para "avaliou e deixou passar" | A passagem limpa é **silenciosa**; nenhum enum novo foi inventado. Lacuna registrada em `hooks/README.md` para o dono do contrato | `evento` é lista fechada lida pelo painel; inventar um valor quebraria a leitura. O propósito declarado (a lista que guia a promoção de aviso para bloqueio) é atendido só com `regra_violada` e `acao_bloqueada` |
| DM-44 | O rastro é gravado em `docs/eventos/`, dentro da árvore de trabalho, e o `branch-limpa` conta arquivo não rastreado como pendência | Os hooks criam `docs/eventos/.gitignore` com `*` na primeira gravação | Sem isso o próprio rastro sujaria a árvore e o `branch-limpa` passaria a barrar **toda** troca de branch. O ignore fica dentro da pasta do rastro para não tocar o `.gitignore` do projeto, que é do time |
| DM-45 | Como identificar o `trabalho_id` corrente dentro de um hook, que não pode ter estado próprio | O `ENTREGA.md` mais recentemente modificado em `docs/entregas/*/` | Regra 6 do contrato: toda decisão sai de arquivo que já existe. O `ENTREGA.md` é o registro que o E0 cria e o E1 atualiza a cada commit |
| DM-46 | O `git-perigoso` barra reescrita de histórico, mas rebase e amend locais são trabalho legítimo | Só barra quando **todos** os commits da branch já estão no remoto (`rev-list --count <upstream>..HEAD` igual a zero) | Reescrever o que ainda é local não afeta ninguém. Barrar isso seria o falso positivo que faz o time desligar o hook — e junto com ele os de segurança |
| DM-47 | O `pr-so-com-portao` roda em todo `git push`, inclusive em repositório sem trabalho da mergex | Sem `ENTREGA.md`, o hook não é acionado | Precisão (regra 1 do desenho): o hook intercepta toda chamada de terminal, e push de outra coisa não pode ser atrapalhado |
| DM-48 | O `revisor-diff` precisa rodar `git diff`, o que exige `Bash` — ferramenta que também escreve | `revisor-diff` recebe `Bash`; o `analista-de-conflito`, que é quem precisa da garantia mecânica de não resolver conflito, recebe **só** `Read`, `Grep` e `Glob` | A garantia forte vai onde ela é o ponto: o CLAUDE.md pede que seja "mecanicamente" impossível o analista resolver conflito. O `revisor-diff` classifica, não altera, e o diff exige o versionador |
| DM-49 | O `commit-por-task` compara o que está em stage com os arquivos declarados; o formato real do `tasks.md` usa lista de fluxo (`cria: [a.ts, b.ts]`), não lista em bloco | O leitor aceita **as duas formas** | O formato real do `expx-schema v1`, conferido nos `tasks.md` existentes, é o de fluxo. Um leitor que só entendesse a forma em bloco não acharia arquivo nenhum e o hook viraria um no-op silencioso |
| DM-50 | Linguagem dos hooks, dado o orçamento de 200 ms por chamada de terminal | `bash` + `jq`, com saída antecipada quando o comando não é do versionador | Medido: ~22 ms no caso comum (comando que nem toca git) e ~131 ms no caminho pesado. Um interpretador maior começaria em ~44 ms só de partida, antes de qualquer trabalho |
| DM-51 | Os scripts precisam rodar no bash do macOS, que é a versão 3.2 | Nada de `mapfile`/`readarray` nem construções de bash 4+ | `mapfile` falha silenciosamente no bash 3.2: o hook não quebra (falha aberta), mas deixa de verificar qualquer coisa — o pior dos dois mundos |
| DM-52 | O `arquivo-fora-do-plano` compara o stage com o plano, mas a própria mergex grava `ENTREGA.md`, `PR.md`, `ATENCAO.md` e `QA-PACOTE.md`, que nenhuma task declara | `docs/entregas/` e `docs/eventos/` são isentos | São a saída da entrega, não o trabalho planejado. Sem a isenção, todo commit de artefato da mergex viraria aviso de escopo |
| DM-53 | Onde os hooks e agentes moram: o contrato descreve uma árvore `plugin/` (hooks/, agents/, skills/), mas este repositório entrega a skill por `.claude/` e `.opencode/` | Seguem a convenção do repositório: `.claude/hooks/`, `.claude/agents/`, `.opencode/plugin/`, `.opencode/agent/` — a mesma que a `stackx` e a `sprintx` já usam | Regra 14: convenção do repositório vence a convenção padrão. A árvore `plugin/` do contrato descreve como o CLI **empacota**, não como cada repositório de skill se organiza |
| DM-54 | O `.expx/hooks.json` do contrato aparece como `{"modos": {"<nome>": "aviso"}}`, mas as skills irmãs já gravam `{"hooks": {"<nome>": {"modo": "aviso"}}}` | O leitor aceita **as duas formas**, preferindo a das irmãs | Divergir do que já está escrito em disco criaria dois formatos no mesmo ecossistema. Aceitar a forma antiga evita quebrar arquivo já editado à mão |
| DM-55 | O OpenCode não tem hooks de configuração: só plugin JS/TS, e o `tool.execute.before` não tem "permite mas avisa" — ou passa em silêncio, ou lança | O plugin **invoca os mesmos scripts** de `.claude/hooks/`; o aviso é represado no `before` e anexado ao resultado da ferramenta no `after` | Duplicar a lógica criaria duas fontes divergindo com o tempo — o pior defeito para uma skill cujo contrato é o produto. A ponte tem só o registro, que é o que de fato difere |
| DM-56 | O Claude Code lê o aviso do hook no stderr; a ponte do OpenCode lê JSON no stdout | Os hooks emitem o aviso **nos dois canais** | Emitir só no stderr faria todo aviso se perder no OpenCode, silenciosamente. Cada harness ignora o canal do outro, então não há ruído |
| DM-57 | O `analista-de-conflito` no OpenCode, cujo formato de agente usa booleanos por ferramenta | `bash: false`, além de `write: false` e `edit: false` | A garantia de que ele não resolve conflito precisa ser mecânica **nos dois harnesses**, não só no Claude Code |
| DM-58 | O hook de descarte casava `git restore` sem distinguir as opcoes, e barrou um `--staged` legitimo em uso real | So e descarte quando mexe na arvore: `--staged` sozinho passa; sem opcao, ou com `--worktree`, barra | `--staged` apenas desfaz o stage e nao destroi nada. Barra-lo e o falso positivo que o desenho manda evitar — quatro casos de regressao entraram na suite |

## Decisões da integração com o memox

| # | Ambiguidade | Decisão tomada | Motivo |
|---|---|---|---|
| DM-59 | O prompt manda acrescentar campos "no kind entrega do `00-schema.md`", mas a mergex não tinha esse arquivo — o contrato do `kind: entrega` vivia dentro de `references/08-registro.md` | Criado `references/00-schema.md`, com o contrato completo do kind; o `08-registro.md` mantém o que se precisa para gravar e aponta para ele | As irmãs têm o contrato num `00-schema.md` próprio; deixar o da mergex embutido na etapa faria o único documento de contrato do ecossistema ter três formatos. Nenhuma regra do `08-registro.md` foi removida |
| DM-60 | O prompt pede o campo `faixa_por_arquivo`; o motor do memox (`memox.py`, linha 618) lê `faixa_atencao`, e é o nome que o `patch-mergex.md` do próprio memox exige | Gravado como **`faixa_atencao`**, com a forma `{arquivo, faixa}` que o prompt descreve | O prompt manda corrigir divergência de nome que quebre o índice. `faixa_por_arquivo` não seria lido por ninguém: o campo existiria e o sinal `faixa_atencao_frequente` continuaria vazio para sempre |
| DM-61 | As três faixas da mergex têm nome em prosa (OLHO OBRIGATÓRIO, LEITURA RÁPIDA, DISPENSÁVEL); o índice do memox usa `alta`/`media`/`baixa` | Mapeamento fixo e declarado: OLHO OBRIGATÓRIO → `alta`, LEITURA RÁPIDA → `media`, DISPENSÁVEL → `baixa`. O YAML leva o vocabulário do índice; a prosa e o `ATENCAO.md` seguem com os nomes de sempre | O painel e o índice leem o YAML; a pessoa lê a prosa. Gravar "OLHO OBRIGATÓRIO" no campo faria `faixa_atencao_frequente` devolver uma string que nenhuma outra skill reconhece |
| DM-62 | O prompt diz que `arquivos_alterados` é "o diff real, não a previsão do plano", mas o campo de mesmo nome na sprintx e na runx **é** a união das tasks | Mesmo nome, semânticas declaradamente diferentes, com a diferença escrita no `00-schema.md`; a divergência entre o diff e o plano continua indo para `desvios` | Renomear na mergex quebraria a consulta única do índice, que é o ponto do campo. As duas respondem "que caminhos este trabalho tocou"; a mergex é só a fonte mais precisa, porque é a única que enxerga o versionador |
| DM-63 | `palavras_chave` existe nas duas irmãs e o prompt manda comparar os três nomes campo a campo | **Não** acrescentado ao `kind: entrega`, com o motivo escrito no `00-schema.md` | Palavra-chave descreve o assunto do trabalho, que a mergex não investigou; gerá-la aqui seria conteúdo novo (regra 7), e termos divergentes dos da origem quebrariam a busca por termo, que casa string exata |
| DM-64 | O prompt manda o critério novo consultar o memox "quando instalado", mas a mergex registra fonte ausente como aviso para todos os outros insumos | Ausência do memox é **silêncio total**: sem aviso, sem fonte ausente, sem menção na saída. Diferente do `PERFIL.md`, cuja ausência continua sendo registrada | `PERFIL.md` ausente significa "dimensão não avaliada", e o revisor precisa saber. O memox é camada opcional que só acrescenta motivo para olhar mais; anunciar a ausência de algo opcional é o ruído recorrente que treina o time a pular o bloco inteiro |
| DM-65 | O prompt não diz quanto a faixa sobe nem qual o teto | O teto é OLHO OBRIGATÓRIO — a faixa mais rigorosa que já existe. Nenhuma faixa nova foi criada | O memox delega a política à mergex. Criar uma quarta faixa para "risco histórico" quebraria as três faixas que são a identidade da skill, os templates, o painel e o contrato do `ENTREGA.md` |
| DM-66 | O prompt não diz o que fazer com `coincidencias_arquivo`, que o patch do memox exclui explicitamente | **Não sobe faixa**, e a exclusão está escrita nos dois lugares que classificam (o reference e o agente `revisor-diff`) | Um arquivo central é tocado por dezenas de trabalhos sem relação causal; usar coincidência subiria a faixa de todo arquivo central, e uma faixa sempre alta não classifica nada |
| DM-67 | Quem consulta o memox: o E3 classifica, mas o E4 também precisa do histórico para a descrição do PR | **Só o E3 consulta.** O E4 reaproveita a saída do E3 e nunca consulta de novo | Duas leituras do mesmo fato podem divergir se o índice for reconstruído entre elas, e quem classifica é o E3. Reconsultar também dobraria o custo sem informação nova |
| DM-68 | O prompt manda disparar a reindexação no E8, mas o memox já traz um hook `Stop` que reindexa ao fim da sessão | Dispara no E8 mesmo assim, **depois** de gravar o `ENTREGA.md`; falha na reindexação vira aviso e não bloqueia | A entrega pode fechar muito antes de a sessão acabar — inclusive numa sessão que nunca chega ao `Stop`. A reconstrução é idempotente e roda em milissegundos |
| DM-69 | O prompt não diz onde `arquivos_alterados` vem quando não há versionador, caso que a regra 15 obriga a tratar | A união dos `arquivos` das tasks concluídas, com o aviso de que a base foi o plano e não o diff | É a melhor aproximação disponível, e é exatamente a semântica que a sprintx e a runx já usam para o campo. Deixar `[]` apagaria a entrega do índice |
| DM-70 | O `revisor-diff` roda em contexto próprio e é ele quem classifica de fato; o prompt só menciona a reference | O critério O9, as quatro regras duras e o formato da justificativa foram para o prompt do agente **nos dois harnesses** (`.claude/agents/` e `.opencode/agent/`) | Uma regra que só existe na reference não é aplicada por quem classifica. Deixá-la só num harness faria a mesma skill classificar diferente conforme onde roda |

## Decisões do estado da barra (`expx-estado` v1)

| # | Ambiguidade | Decisão tomada | Motivo |
|---|---|---|---|
| DM-71 | O contrato canônico do CLI estava acessível na máquina desta execução; o prompt manda o contrato vencer o resumo em caso de divergência | **Não houve divergência.** O contrato foi lido, conferido campo a campo contra o resumo do prompt e copiado para `docs/contrato/CONTRATO-expx-estado.md` deste repositório | O resumo do prompt é fiel ao contrato nos dois campos da mergex (`branch`, `pr_estado`), no enum, na escrita atômica e na regra de preservar campo alheio. A cópia local torna a skill autocontida, como o prompt exige |
| DM-72 | O prompt lista quatro references a alterar (E0, E7, E9) e o momento "ao concluir ou abandonar o trabalho", que não cai em nenhum deles | A limpeza dos dois campos foi para `references/08-registro.md` (E8) | O E8 é a etapa que fecha a entrega, e o contrato manda os dois campos voltarem a `null` quando o trabalho termina. Deixar de escrever isso deixaria a barra mostrando para sempre a branch do último trabalho — o defeito que o campo existe para evitar |
| DM-73 | O que fazer com `pr_estado` no E0 quando a branch é **retomada** e o PR já havia sido aberto numa sessão anterior | `pr_estado: null` também na retomada; o E7 o repõe quando rodar | O E0 não consulta o serviço de hospedagem e não tem como saber o estado real do PR. Preservar um `rascunho` velho seria afirmar sobre o serviço sem ter olhado; `null` é o valor honesto para "ainda não verificado nesta passagem" |
| DM-74 | O prompt manda gravar `merged` no E9 "apenas quando o PR mergeado for o do trabalho atual", sem dizer como reconhecê-lo | A `pr_url` do `ENTREGA.md` daquele PR casa com a do `ENTREGA.md` do trabalho nomeado em `trabalho` no próprio `estado.json`. Correspondência incerta, `trabalho: null` ou `estado.json` ausente: **não grava** | O E9 percorre PRs de vários trabalhos e de outras pessoas, e a barra mostra um trabalho só. Na dúvida, não gravar deixa a barra levemente desatualizada; gravar errado faz a barra afirmar que o trabalho em andamento foi mergeado |
| DM-75 | O contrato não diz o que fazer quando `.expx/estado.json` existe mas está corrompido ou ilegível | Sai em silêncio, sem gravar. Não conserta e não substitui por objeto novo | O arquivo é compartilhado: substituí-lo apagaria trabalho, fase, task e orçamento das outras skills. Um arquivo derivado quebrado se resolve sozinho na próxima gravação de qualquer skill; destruí-lo para "consertar" custa mais do que a barra vale |
| DM-76 | O prompt manda gravação atômica, sem prescrever a ferramenta | Temporário no **mesmo diretório** (`.expx/estado.json.tmp`) e `mv -f` / `os.replace`; o procedimento pronto usa `python3`, com `jq` como alternativa | O temporário precisa estar no mesmo sistema de arquivos, senão a renomeação vira cópia e deixa de ser atômica. `jq ... > estado.json` foi proibido explicitamente: o redirecionamento trunca o arquivo antes de lê-lo, e é o erro exato que a escrita atômica existe para evitar |
| DM-77 | Se a nova gravação deveria virar regra inviolável, ao lado das 20 existentes | **Não.** As 20 regras estão idênticas, na mesma ordem e com o mesmo texto | O prompt proíbe. E a proibição está certa: regra inviolável é o que não pode falhar, e este arquivo é derivado, descartável e sem leitor que dependa dele. Uma regra inviolável sobre ele autorizaria interromper trabalho por causa da barra — o oposto do que o contrato pede |
| DM-78 | Se algum hook deveria passar a manter os campos, já que os hooks interceptam commit e push | **Nenhum hook foi tocado.** O `branch-limpa` continua lendo `git status --porcelain` diretamente | O contrato é explícito: nenhum hook usa o `estado.json` como fonte. Um hook que lesse arquivo derivado passaria a barrar ou liberar por informação possivelmente velha — e o `branch-limpa` nasce em modo de bloqueio, onde um falso positivo trava o trabalho de todo mundo |
| DM-79 | O nome da branch pode ser longo e a barra tem largura limitada | Gravado **completo**, sem corte | Quem sabe a largura disponível é o script da barra, que roda no terminal; a skill não sabe. Cortar aqui destruiria informação que o consumidor talvez conseguisse mostrar inteira, e um corte no meio de um `OC-ID` é pior que nenhum corte |

## P0 — compatibilidade com a sprintx atual (worktree, path canônico, suíte por task)

A sprintx evoluiu depois que este contrato foi escrito: o caminho canônico das features passou
a ser `docs/sprintx/features/<slug>/`, e a regra 21 dela deu à F1 a posse da branch
`feature/<slug>` e de um `git worktree` por feature — aberto na F1, muito antes da F6. O E0,
escrito para um mundo em que ninguém tinha criado branch ainda, procurava a pasta antiga,
tentava criar ou trocar a branch e exigia árvore limpa mesmo quando já estava na branch certa.
Nenhum caminho de execução funcionava. Este bloco registra o que mudou para a mergex **adotar**
o trabalho da sprintx atual, sem mudar o papel de nenhuma das duas.

Regra de ouro: nenhuma das 20 regras invioláveis foi renumerada, removida ou enfraquecida; o
E9 e o `/mergex-revisar` não foram tocados; nenhuma etapa nova foi criada.

| # | Ambiguidade | Decisão tomada | Motivo |
|---|---|---|---|
| DM-80 | Duas skills criam `feature/<slug>`: a F1 da sprintx (com worktree) e o E0 da mergex. Quem é dono? | **A F1 da sprintx é a dona** da branch e do worktree; o E0 **adota** o que encontra e só cria branch quando não existe nenhuma. Nunca uma segunda branch para o mesmo trabalho | Quem cria primeiro é quem tem a informação: a F1 abre a feature antes de existir código, e o worktree é dela. Disputar a posse produziria duas branches para uma feature — ou um `git switch` que o git recusa, porque a branch já está em uso em outro worktree |
| DM-81 | O E0 procurava `docs/<slug>/ORQUESTRADOR.md`, caminho que a sprintx trata como formato antigo | Busca em ordem: `docs/sprintx/features/<slug>/` (canônico) e, só se ele não existir, `docs/<slug>/`. Vale para E0, V6, V7, V9 e para os comandos. **Nunca mover pasta legada** | A sprintx continua trabalhando nas pastas antigas onde elas estão (DS-55 dela); mover por conta própria quebraria links e histórico do repositório do usuário. Procurar nas duas é o que faz a mergex funcionar nos dois mundos sem migrar nada |
| DM-82 | O E0 não tinha como receber a base de quem o chama, e a precedência começava no `CONVENCOES.md` | **Base informada pelo chamador** passa a ser a precedência mais alta, validada com `git rev-parse --verify`; ref inexistente **para o E0**, com a base pedida nomeada | É a única fonte que carrega informação que o repositório não tem: de qual árvore este trabalho precisa sair. E cair em silêncio para `origin/HEAD` faria o trabalho nascer de uma árvore diferente da pedida — ninguém descobriria até o diff da entrega vir errado |
| DM-83 | A regra 2 ("nunca criar ou trocar branch com alteração pendente") estava implementada como "árvore limpa sempre", o que barrava a adoção: na F6, os artefatos de F1 a F5 estão na árvore, ainda não commitados | Árvore limpa é pré-condição **de criar ou trocar branch**, não de reconhecer que o processo já está na branch certa. Adotar não move nada e não exige árvore limpa | É o que a regra 2 diz literalmente. O risco que ela existe para evitar é uma troca de branch carregar ou destruir trabalho não salvo; adotar não troca. A proteção continua integral nos casos em que a mergex de fato mexe na posição da árvore — inclusive contando arquivo não rastreado (DM-26) |
| DM-84 | Branch do trabalho já existente e em uso por **outro** worktree: `git switch` falha, e forçar significaria mexer na árvore de alguém | O E0 localiza com `git worktree list --porcelain`, informa o caminho e encerra. Nunca troca, nunca remove worktree, nunca força | A mergex não mexe na árvore de ninguém (mesma disciplina de "nunca descartar alteração local"). O caminho no aviso é acionável: continuar de lá é uma linha de comando, e lá o E0 adota normalmente |
| DM-85 | O E1 e a V2 exigiam `suite: verde` por task, mas a sprintx fecha task com `suite: parcial` (subconjunto afetado) e cobra a suíte inteira ao fechar a sprint | Task concluída: `parcial` e `verde` são válidas; `vermelha` e `nao_executada` barram. A V2 passa a cobrar, além disso, a **evidência da suíte inteira no fechamento de cada sprint** (FALHA se registrada vermelha, AVISO se ausente) | Exigir `verde` por task obrigaria a skill de origem a escrever um resultado que ela não produziu — o portão passaria a premiar registro falso, que é o oposto do que ele existe para fazer. TDD não muda: os dois testes por task continuam obrigatórios, e a suíte inteira continua exigida, no ponto em que ela de fato roda |
| DM-86 | O plano, as decisões, os bloqueios e o `FECHAMENTO.md` da sprintx não estão em `arquivos` de nenhuma task: o E1 os tratava como desvio de escopo, e eles nunca chegavam ao histórico — sumindo junto com o worktree | **Artefatos de método do próprio trabalho** (a pasta do trabalho, canônica ou legada, mais `docs/entregas/<trabalho_id>/`) entram no commit e nunca são desvio, em dois momentos determinísticos: no commit da task que fechou, e num commit de artefatos imediatamente antes do push. Arquivo de produto fora do plano continua sendo desvio | Sem isso, o registro do trabalho existe só na árvore — e a árvore da feature é um worktree que será removido. A isenção é **da pasta do trabalho identificado**, nunca de `docs/` inteiro: a pasta de outro trabalho continua sendo desvio, que é como se percebe escopo invadido. O commit de artefatos é o E1 chamado num segundo momento, **não uma etapa nova**: as nove etapas seguem as mesmas, na mesma ordem |
| DM-87 | `expx_raiz` procurava `[ -d "$dir/.git" ]`; num `git worktree`, `.git` é arquivo, e a busca subia demais — de um subdiretório do worktree, devolvia o diretório errado, e `docs/entregas/`/`docs/eventos/` iriam para o lugar errado | Pergunta primeiro ao versionador (`git rev-parse --show-toplevel`), que acerta em checkout, worktree e subdiretórios dos dois; o fallback sem git sobe procurando `.git` com `[ -e ]` | Mesmo achado que a sprintx registrou na DS-106 dela. Perguntar ao git é reutilizar o que já existe em vez de reimplementar a descoberta de raiz; o fallback preserva o comportamento fora de repositório e sem git no PATH |
| DM-88 | O registro da adoção poderia virar chave nova no `kind: entrega` (por exemplo `branch_origem`) | Fica na **prosa** do `ENTREGA.md` (e na linha que o E0 grava no `ORQUESTRADOR.md`); nenhuma chave nova no frontmatter | Chave nova num kind que o painel lê é mudança de contrato compartilhado, e esta informação é para quem abre o arquivo — não há consumidor de máquina para ela. Mesmo critério da DM-23 e da DM-63 |
| DM-89 | A suíte de hooks depende de `jq`; sem ele, todo hook sai cedo por 0 e a suíte reportava "ok" onde não verificara nada | Casos dependentes de `jq` são **PULADOS** e contados como tal fora de CI; com a variável `CI` definida, a ausência de `jq` **aborta** a suíte | "Passou" sem ter verificado é pior que falhar: esconde regressão em máquina de desenvolvedor. Pular declarando, e falhar no ambiente onde a cobertura é obrigatória, mantém as duas propriedades |
| DM-90 (refina DM-86) | A isenção dos artefatos de método precisava saber qual é o trabalho corrente, e o hook reutilizou `expx_trabalho_id` — que escolhe o `ENTREGA.md` mais recente. Numa árvore integrada (`docs/entregas/ft-01`, `ft-02`, `ft-03` no mesmo checkout), recência não prova qual trabalho está em curso | Resolver próprio e determinístico, `expx_trabalho_atual_por_branch`: branch ativa casada com o `branch:` do frontmatter de **exatamente um** `ENTREGA.md`. Zero, dois ou mais, HEAD destacado, arquivo ilegível ou qualquer dúvida: trabalho não determinado e **nenhuma isenção**. Nunca mtime, nunca `estado.json`, nunca heurística de slug. `expx_trabalho_id` fica **intacto**, porque continua sendo o helper histórico do destino do rastro | Errar o trabalho corrente isentaria a pasta errada — e a isenção existe justamente para não esconder invasão de escopo. A identidade do trabalho já está declarada pelo próprio `ENTREGA.md`, no campo `branch`: casar a branch ativa com esse campo é ler o que a entrega afirma, em vez de inferir por um sinal que não é dela. Mudar a semântica do helper antigo seria regressão lateral em quem depende dele (o rastro), então o resolver novo convive com ele |

## P0 — o estado final da entrega precisa sobreviver ao worktree

Enquanto o worktree continuava sendo a fonte principal depois da entrega, o E8 podia reescrever
o `ENTREGA.md` e deixá-lo no disco. No fluxo integrado, não: a skill de origem abre um
`git worktree` por feature e o remove depois, e quem integra a branch integra **commits**. Um
registro final que só existe na árvore não chega à integração e some com o worktree.

| # | Ambiguidade | Decisão tomada | Motivo |
|---|---|---|---|
| DM-91 | O E8 grava o estado final (`estado: entregue`, `push_feito`, `pr_url`, `pr_estado`, `entregue_em`) **depois** do E6 e do E7, e o contrato mandava essa gravação esperar "o próximo commit de artefatos" — que pode nunca existir | O E8 passa a ter um **fechamento final**: commita os artefatos de método deste trabalho ainda sujos e publica esse commit pelo mesmo princípio conservador do E6. O commit pré-E6 continua existindo: ele carrega o que precisa existir **antes** da publicação (`FECHAMENTO.md`, `PR.md`, `QA-PACOTE.md`, `ATENCAO.md`); o do E8 carrega o que só é conhecido **depois** dela | Sem isso, `feature/<slug>` aponta para um commit cujo `ENTREGA.md` está defasado: a integração leva registro velho, a recuperação do SHA pela entrega fica inconsistente, e a evidência morre com o worktree. É defeito de persistência de contrato, não preferência de estilo |
| DM-92 | O commit de fechamento poderia ser lido como uma task a mais, ou como etapa nova (E10) | **Não é task e não é etapa nova**: é o E1 no formato que ele já usa, chamado no fim do E8. Não entra na lista `commits`, não altera arquivo de produto, entra por caminho explícito e passa pela mesma varredura de segredo | A lista `commits` é a interface do painel com as tasks; poluí-la com artefato de método faria a entrega parecer ter mais trabalho do que teve. Criar etapa nova duplicaria restrições de acionamento sem necessidade |
| DM-93 | `push_feito` é gravado num arquivo que ainda vai virar commit, e esse commit ainda vai ser publicado — a circularidade tentava um estado intermediário | `push_feito: true` passa a afirmar "a publicação foi executada com sucesso neste fluxo **e** o commit que carrega este registro está no remoto", resolvida pela ordem: grava → commita → publica → confirma. Deu certo, nada muda depois; falhou, um commit corretivo grava `push_feito: false` com a prosa correspondente | Um `pending` novo contaminaria o `expx-schema` compartilhado por um detalhe de execução da mergex. E `true` que não corresponde ao remoto quebra exatamente quem confia no registro para achar a entrega |
| DM-94 | Sem remoto, ou com o push do E6 bloqueado, o fechamento poderia ser considerado inaplicável | O commit final **acontece mesmo assim**, localmente; só a publicação é pulada. Sem versionador é que nada roda | O fast-forward local continua possível em projeto sem hospedagem, e a evidência do trabalho não pode depender de haver remoto |
| DM-95 | O que fazer com o que sobra sujo que **não** é deste trabalho (produto fora do plano, pasta de outro trabalho, derivados) | Nunca entra no commit final: produto fora do plano continua sendo desvio (regra 4), e derivado não versionado (rastro, `.expx/`, índice do memox) não é artefato da entrega. O que sobrar é nomeado no relatório | Varrer isso para dentro do commit "para deixar a árvore limpa" seria exatamente a invasão de escopo que a regra 4 existe para impedir |

**O que invalidaria estas decisões:** se a integração passasse a ler a árvore de trabalho em vez
do histórico (nenhuma das skills faz isso), ou se o `ENTREGA.md` deixasse de ser versionado e
virasse artefato derivado, reconstruível a partir de outra fonte. Enquanto ele for o registro da
entrega e a integração for por commit, o fechamento final é obrigatório.

## P0 final — bloqueio persistido e E0 idempotente no replanejamento

A auditoria cruzada, depois da persistência final do E8, achou três contradições que só aparecem
no fluxo integrado: o portão que "encerrava tudo" enquanto o E8 prometia persistir o bloqueio, o
`push_feito` com duas definições, e o E0 sendo chamado de novo a cada F6 — inclusive depois de um
replanejamento — sobre um `ENTREGA.md` que já existia.

| # | Ambiguidade | Decisão tomada | Motivo |
|---|---|---|---|
| DM-96 | `02-prontidao.md` mandava "encerrar o fluxo da mergex" no `BLOQUEADO`, e `08-registro.md` já previa `estado: bloqueado` persistido — as duas coisas não podiam ser verdade | `BLOQUEADO` **encerra as etapas de entrega** (E3 a E7 não executam) e segue **apenas ao E8**, em fechamento bloqueado: grava `estado: bloqueado`, preserva `portao: bloqueado`, persiste por commit e **não publica a branch** | Ir ao E8 não é continuar a entrega: é finalizar e persistir o bloqueio. Sem isso, o motivo da parada morre com o worktree e a próxima sessão não sabe por que o trabalho parou. O portão continua barrando a entrega, que é o que ele existe para fazer |
| DM-97 | `push_feito` tinha duas definições: "o E6 confirmou o remoto" na tabela campo a campo, e "o commit do registro final está no remoto" no fechamento | Um significado **por estágio**: no E6, `true` afirma a sincronia daquele estágio; **ao encerrar o E8**, afirma que o HEAD final está em `origin/<branch>`. O E8 **revalida** e grava `false` se a publicação final não aconteceu | Nenhum enum novo e nenhuma mudança de schema; o campo continua booleano. A alternativa — manter `true` depois de uma publicação final falha — faria o registro mentir exatamente para quem confia nele para achar a entrega |
| DM-98 | O E0 roda no início de **toda** F6, inclusive depois de replanejamento, e o contrato mandava "criar" o `ENTREGA.md` | O E0 é **idempotente**: CASO 1 cria; CASO 2 (mesmo trabalho, mesma branch) **retoma** — `estado: aberto`, `portao: null`, `push_feito: false`, preservando `commits`, `criado_em`, desvios ainda verdadeiros e a prosa válida; CASO 3 (arquivo declara outra branch) para e relata | Recriar apagaria o histórico de execução de um trabalho que voltou ao começo da entrega, não ao começo do mundo. O caso 3 é conservador porque reaproveitamento de `trabalho_id` e troca de branch por fora são decisão humana |
| DM-99 | Preservar `commits` na retomada levanta a dúvida do `task` id reaproveitado pelo plano refeito | `commits` é **histórico de execução**: um commit por fechamento de task **em cada execução**, nunca "id único para sempre". Item antigo nunca é apagado; um id pode reaparecer com **outro SHA**; mesmo id e mesmo SHA é duplicata e não entra | Compatível com o schema, que descreve `commits` como lista ordenada, sem exigir unicidade (`00-schema.md`), e nenhum consumidor deste repositório indexa por `task` id. Não foi preciso campo novo. Se algum leitor externo exigir unicidade, a decisão precisa ser revista antes de valer para ele |
| DM-100 | O fechamento prometia "árvore limpa", mas desvio de produto **não pode** ser commitado | A garantia passa a ser precisa: **ao sair do E8, nenhum artefato de método legítimo deste trabalho fica sem persistir**. Arquivo de produto fora do plano não entra, não é apagado, e continua aparecendo em `git status` | Prometer árvore inteira limpa obrigaria a varrer o desvio para dentro do commit — exatamente a invasão de escopo que a regra 4 existe para impedir. O desvio continua pertencendo a quem precisa resolvê-lo |

**O que invalidaria estas decisões:** um leitor do `kind: entrega` que trate `commits[].task` como
chave única (DM-99), ou um fluxo em que o portão bloqueado precise publicar a branch — hoje
proibido pelo próprio E6.

## P0 E2E — HISTORICO global da sprintx e os dois formatos de plano

O primeiro E2E real (buildx → sprintx → mergex) parou na FT-01, no E2/V9. Uma das causas era da
sprintx e já foi corrigida lá. A outra é desta skill: `docs/sprintx/estimativas/HISTORICO.md`
aparecia como arquivo de produto fora do plano. A sprintx (0dddbfa) declarou formalmente o dono
do arquivo; o que faltava aqui era consumir essa declaração — e, junto dela, o formato de sprint
condensada que a skill irmã formalizou.

| # | Ambiguidade | Decisão tomada | Motivo |
|---|---|---|---|
| DM-101 | `HISTORICO.md` é método, mas mora **fora** da pasta da feature — e a isenção de método da mergex era por pasta do trabalho | Exceção **exata**: o caminho literal `docs/sprintx/estimativas/HISTORICO.md`, e **somente quando a origem é a sprintx**. Nada sob `docs/sprintx/estimativas/**`, nada sob `docs/sprintx/**`, nada equivalente na runx. Vale no E1, na V9, no hook de escopo e no E8 | Um curinga isentaria o que a V9 existe para pegar: invasão real de escopo fora da pasta do trabalho. A exceção exata resolve o caso declarado sem abrir a porta para o resto |
| DM-102 | Em que commit o `HISTORICO.md` entra | No caminho `PRONTO`, no **commit de artefatos pré-E6** — é o ponto normal de versionamento. **Nunca no commit de uma task**: se estiver sujo durante um E1, não é desvio e fica para o pré-E6. No caminho **bloqueado**, onde não existe commit pré-E6, ele entra no **commit final do E8**, que persiste localmente e não publica | Um commit de task contém a task e o método daquele trabalho, não a memória global acumulada. E no bloqueio, sem o fechamento do E8, a retomada perderia a memória que a sprintx já havia gravado |
| DM-103 | Na retomada depois de bloqueio, o `HISTORICO.md` já commitado aparece em `git diff <base>...HEAD` e voltaria a reprovar a V9 | A exclusão da V9 **não depende de ele estar sujo agora**: vale também para o que já está no histórico da branch | A regra é sobre a natureza do arquivo (método global declarado), não sobre o momento em que ele foi escrito. Amarrá-la ao estado sujo faria o segundo ciclo de replanejamento falhar onde o primeiro passou |
| DM-104 | A sprintx passou a ter dois formatos de sprint (condensado `kind: plano` e três arquivos), e cada etapa da mergex poderia resolver isso do seu jeito | **Uma regra única**, escrita em `references/integracao/sprintx.md` ("Como ler uma sprint da sprintx"); E1, E2, E4 e E5 apontam para ela. As tasks vêm sempre da chave `tasks`, com os mesmos campos e o mesmo rigor; `sprint.md` e `fases.md` **não são exigidos** no condensado, e a ausência deles não vira aviso | Descrever a regra em cada etapa criaria quatro descrições que divergem na primeira manutenção — foi exatamente o bug que a sprintx encontrou entre F3 e F4. Nenhum schema novo: o contrato de origem já define os dois formatos |
| DM-105 | A V2 procurava a evidência da suíte inteira em `sprint-NN/sprint.md`, que não existe no condensado | A busca virou **format-aware**: três arquivos → `sprint.md` e o relatório da F6; condensado → a chave `sprint` do próprio `tasks.md` e o relatório. Ausência de `sprint.md` no condensado **não** é aviso nem falha; a ausência de **evidência de execução** continua valendo o que já valia | Exigir um arquivo que o formato não tem barraria plano válido. O que o portão cobra é a execução registrada, não o nome do arquivo onde ela mora |

**O que invalidaria estas decisões:** a sprintx deixar de declarar o `HISTORICO.md` como artefato
global versionado (se ele virar estado local, a exceção sai daqui junto); um terceiro formato de
sprint (a regra única passaria a ter três ramos, e o lugar de mudá-la continua sendo um só); ou a
runx declarar um artefato global equivalente — que precisaria de contrato próprio, porque esta
isenção é da sprintx e não se estende por analogia.

## P1 — atenção humana dos artefatos de método (E3)

O segundo E2E real (buildx → sprintx → mergex) passou inteiro, mas o E3 deu 17 arquivos em OLHO
OBRIGATÓRIO de 18 nas duas features, e 15 deles vinham só do padrão: plano, decisões, base,
auditoria, fechamento, `HISTORICO.md` e o próprio `ENTREGA.md` não batiam em critério nenhum. O
padrão conservador estava certo; faltava critério. Com quase tudo em leitura linha a linha, a
classificação deixava de dizer onde olhar.

| # | Ambiguidade | Decisão tomada | Motivo |
|---|---|---|---|
| DM-106 | Artefato de método (decisão, plano, base, registro) não tinha critério no E3 e caía no padrão OLHO OBRIGATÓRIO | Dois critérios novos, **L4** (artefato de método reconhecido, de decisão, plano ou registro) e **D4** (artefato de método reconhecido, mecânico, com a prova mecânica aprovada). Artefato de método **não é automaticamente de baixo risco, nem automaticamente OLHO OBRIGATÓRIO**. **Os critérios O têm precedência**: num artefato de método, O2/O3/O4/O5/O7 batem na **origem** da decisão de risco (`D-NN`, `PR-NN`) ou quando ele contém dado pessoal real; o que só repete ou executa a decisão não dispara O de novo. O6 não se aplica a artefato de método. **O padrão conservador continua**: o que não é reconhecido segue a classificação de sempre | Tirar o artefato de método do padrão sem apagar o risco. A decisão de risco continua sendo lida linha a linha, no lugar onde nasce, e o resto do método vai para conferência de intenção ou para a máquina |
| DM-107 | Como reconhecer artefato de método sem abrir brecha para esconder produto ou regra de negócio | **Catálogo fechado e específico**, por origem: caminho exato dentro da pasta **deste** trabalho (resolvido pela branch, o mesmo resolvedor do hook de escopo), origem confirmada pela pasta **e** pelo `expx_tool` do `ENTREGA.md`, `ORQUESTRADOR.md` deste trabalho na pasta, `kind` conferido quando o contrato de origem define um (e ausente quando não define), arquivo não executável. Os únicos segmentos variáveis são `sprint-NN` e `base/<area>.md` listado no índice. **Nenhum curinga**: nem `docs/**`, nem `docs/sprintx/**`. `expx_tool` escrito num arquivo não reconhece nada. A runx tem catálogo próprio e não herda nomes da sprintx | Um curinga transformaria a pasta da feature em zona dispensável: qualquer arquivo colocado lá escaparia da revisão. Cada sinal sozinho pode ser forjado; todos juntos, na pasta do trabalho corrente, não sem forjar também o registro da entrega |
| DM-108 | Quem decide L4 e D4, e onde mora a ordem | Um script da skill, `scripts/classificar-atencao.sh`: recebe do agente os critérios O, L1–L3 e D1–D3 confirmados, aplica a ordem O → L1–L3 → L4 → D1–D3 → D4 → padrão e é **o único que concede L4 e D4**. L4/D4 passados na entrada são ignorados. O agente continua levantando a evidência. Sem o classificador, a ordem é aplicada à mão **sem L4 e D4** | Reconhecimento por prosa não é testável nem auditável. O script torna a precedência e o catálogo verificáveis pela bancada, com mutação. Prosa e script ficam amarrados pelo validador de contrato (ordem, catálogo linha a linha, bloco da regra idêntico no reference e no agente) |
| DM-109 | Qual artefato de método vai para L4 e qual vai para D4 | **L4:** decisões, premissas da buildx, estimativa, orquestrador, auditoria, fechamento, plano (`tasks`/`plano`, `sprint`, `fases`), lacunas e base listada no índice; na runx, ocorrência, causa raiz, orquestrador e QA. **D4, só com prova mecânica:** bloqueios com `bloqueios: []` e sem `B-NN`; `base/00-INDICE.md` com toda área listada existindo; `HISTORICO.md` que, contra a base, só acrescenta entradas **deste** trabalho sem reescrever linha existente (fora `atualizado_em`); `ENTREGA.md`, `PR.md`, `QA-PACOTE.md` e `ATENCAO.md` deste trabalho, que são a saída da própria mergex. **Prova que falha manda para L4, nunca para DISPENSÁVEL** | D4 exige o que D1–D3 já exigem: que a máquina tenha provado. Plano e decisão escritos por agente carregam escolha; não são "gerados" (D3 continua sendo geração determinística declarada) |
| DM-110 | Como provar que a política nova não esconde risco e não é otimização de contagem | Bancada `scripts/ci/test-atencao-metodo.sh` com os cenários A–H e a **reprodução do E3 do E2E real** (fixture copiada, a original intocada), conferindo faixa, critério e **motivo** por arquivo. Resultado no E2E, por feature: 4 OLHO OBRIGATÓRIO (os dois arquivos de produto, e `00-DECISOES.md` e `BUILDX-PREMISSAS.md`, que são origem de decisão de contrato público e regra de negócio), 10 LEITURA RÁPIDA, 4 DISPENSÁVEL; nenhum arquivo no padrão. Mutações em `scripts/ci/mutacao-atencao-metodo.sh` provam que a bancada falha quando a regra é removida, invertida ou afrouxada. **Nenhuma meta de percentual** | Uma feature inteira de autorização continua legitimamente quase toda OLHO OBRIGATÓRIO. O que importa é o motivo de cada faixa, não a distribuição |

**O que invalidaria estas decisões:** a sprintx, a runx ou a buildx mudarem o nome, o caminho ou o
`kind` de um artefato do catálogo (o catálogo e a bancada mudam juntos, no script e no reference);
o `HISTORICO.md` deixar de ser append-only na sprintx (a prova do D4 precisaria ser refeita); ou a
runx passar a usar `docs/runx/ocorrencias/`, layout que o leitor do expxdev já conhece mas o
contrato da runx ainda não escreve — até lá, esse layout não é reconhecido e segue o padrão.

## P0.2-A4 — o portão registra as falhas e o E8 deriva a causa

O `ENTREGA.md` terminal dizia **que** o portão barrou (`estado: bloqueado`, `portao: bloqueado`),
mas não **por quê**: o motivo ficava na prosa e na memória da sessão. No piloto, um bloqueio com
V1 e V7 (o B-01 pedia alterar arquivo fora do ownership) foi chamado à mão de `falha_tecnica` —
classificação que nenhum artefato sustentava. A buildx precisa ler a causa por `git show` no HEAD
da feature, sem interpretar texto.

| # | Ambiguidade | Decisão tomada | Motivo |
|---|---|---|---|
| DM-111 | Qual enum de `causa`, sem inventar por intuição | **Uma causa por verificação do portão** (V1–V10), e nenhuma outra. Cada valor passou as cinco perguntas: a mergex observa sozinha (é o resultado da própria verificação), é determinístico, duas execuções sobre a mesma evidência dão o mesmo valor, a evidência está commitada (`falhas_portao` no mesmo arquivo, e os artefatos lidos pelo E2 no histórico) e é causa do bloqueio **do ponto de vista do portão**. Classificações pelo conteúdo do bloqueio (`falha_tecnica`, `decisao_humana`, "fora do ownership") **não passam**: dependem de ler a narrativa do `B-NN`, que não tem campo tipado | A mergex é dona da causa observável do portão. Traduzir para classe de pendência é da buildx; classificar o `B-NN` pelo tipo exige que a sprintx o grave tipado |
| DM-112 | Como registrar as falhas | Chave nova **`falhas_portao`**, gravada pelo E2 junto com `portao`: as verificações com FALHA, na numeração do portão. Verificação que não pôde rodar entra como **`vN_sem_prova`** — o contrato já a marcava FALHA ("ausência de prova não é prova"), mas sem distingui-la de defeito provado | A causa precisa de evidência no mesmo arquivo para ser recomputável e auditável (`--validar` recalcula e compara). Sem o sufixo, um "não consegui verificar" seria lido depois como defeito provado |
| DM-113 | Várias falhas ao mesmo tempo | **Precedência fixa**: V10; depois V6–V9; depois V1–V5, cada grupo na numeração. A primeira presente decide. V10 primeiro pela regra 5. V6–V9 são fatos que existem independentemente de a execução ter terminado; V1–V5 leem o registro da execução, que fica incompleto justamente quando um daqueles fatos parou o trabalho (o contrato do E2 já diz que task `bloqueada` aponta o `B-NN`, e que QA ausente é fluxo que não chegou ao E4 da runx). Todas as falhas continuam em `falhas_portao` | Uma regra que qualquer leitor recalcula. A ordem não é juízo de gravidade; entre falhas independentes do mesmo grupo, a numeração é convenção declarada |
| DM-114 | Bloqueado sem causa determinável | Valor **`indeterminada`**, nunca `null` e nunca uma causa escolhida: quando a primeira verificação da ordem presente em `falhas_portao` é `_sem_prova`. Bloqueado com `falhas_portao: []` é registro inconsistente: o script recusa e o E2 roda de novo | A decisão de desenho pede bloqueado com causa não nula. `indeterminada` é a representação mínima disso, e quem lê a trata como causa não commitada |
| DM-115 | Quem grava `causa`, e quando | O **E8**, na mesma gravação de `estado: bloqueado` e antes do commit de fechamento, pelo script `scripts/causa-do-portao.sh --derivar`, validado com `--validar`. O E2 não grava causa: entre o E2 e o E8, `estado: aberto` tem `causa: null` | "Aberto → causa null" vale sempre. A causa entra no HEAD pelo mesmo commit que persiste estado, portão e push |
| DM-116 | `ENTREGA.md` anterior às chaves | **Leitura histórica aceita** (`--validar-historico` → `causa=ausente`: não commitada, nunca inferida); **gravação nova inválida** (`--validar` exige as duas chaves); **sem migração em massa** — a regra de migração existente acrescenta as chaves na próxima gravação, que só ocorre onde o valor é `null`/`[]` por definição. Bloqueio legado nunca ganha causa retroativa. Uma chave sem a outra reprova nos dois modos | Inferir a causa de um bloqueio antigo seria ler a prosa — exatamente o que este contrato proíbe |

**O caso do piloto.** `falhas_portao: [v1, v7]` → `causa: bloqueio_aberto`. O texto do B-01 não
muda o valor, e a mergex não o transforma em `falha_tecnica`. Distinguir "fora do ownership" de
outro tipo de bloqueio exige o `B-NN` tipado na sprintx (A5) e a tradução na buildx (A6); nenhuma
das duas é pré-requisito desta causa.

**O que invalidaria estas decisões:** uma verificação nova no portão (ganha linha na tabela e
posição na ordem, no script e no schema juntos — foi o que aconteceu na P0.2-C3, com a V11); a
sprintx gravar o `B-NN` com tipo — o que permitiria, em outra entrega, refinar `bloqueio_aberto`
a partir de campo tipado, nunca da prosa; ou um leitor que precise de todas as falhas, e não só
da causa — ele já tem `falhas_portao`.

## P0.2-C3 — a V11 cobra o commit da task concluída

O E1 promete, em três lugares do contrato, que a task que ficou sem commit "o E2 vai barrá-la":
quando a varredura de segredo aborta o commit, quando a branch está errada, quando o hook do
versionador recusa. A promessa não se cumpria. V1 lê `status`, V2 lê `suite`, V3 lê os testes,
V9 lê o diff — e **nenhuma** das dez cruzava `tasks.md` com `ENTREGA.commits`. Uma task podia
estar `concluida`, com suíte verde e os dois testes declarados, sem nenhuma evidência de que
alguma vez virou commit. O portão não via.

| # | Ambiguidade | Decisão tomada | Motivo |
|---|---|---|---|
| DM-117 | O que exatamente a V11 pergunta, sem virar uma segunda V1 nem uma V2 ampliada | **Uma pergunta só:** para cada task `concluida`, existe pelo menos um item de `ENTREGA.commits` com `task` **exatamente** igual ao id dela e `commit` válido? A V11 **não** lê `suite`, **não** lê `teste_integracao`/`teste_funcional` e **não** olha o status das não concluídas. Task `pendente`, `em_andamento` ou `bloqueada` **não é alvo positivo**: ela não tem commit porque não fechou, e a razão é da V1 | Falha independente e diagnosticável. Ampliar a V2 ou reaproveitar a V1 daria duas razões pelo mesmo sintoma e uma causa que não aponta para onde corrigir |
| DM-118 | `OK` ou `n/a` quando não há nenhuma task concluída | **`n/a`**, com motivo declarado — a convenção real de V1–V10, onde `n/a` é "não se aplica a este trabalho" (V4 fora de bug da runx, V5 na sprintx, V8 sem `PERFIL.md`). Havendo pelo menos uma concluída, é `OK` para o subconjunto concluído. `versionado: false` também é `n/a`: o schema já define `commits: []` sem versionador, e cobrar commit ali seria reprovar todo repositório sem versionador | `OK` sobre conjunto vazio afirmaria uma prova que ninguém produziu. `n/a` distingue "verifiquei e está certo" de "não havia o que verificar" — que é a razão de a tabela nunca omitir linha |
| DM-119 | Uma task pode aparecer mais de uma vez em `commits` (replanejamento). Quantos itens a V11 exige? | **EXISTE pelo menos um**, nunca exatamente um. Duplicidade **não** é falha da V11, e ordem e sequência dos itens também não são assunto dela. Um item malformado ao lado de um válido não estraga a prova: basta que **um** item prove | `commits` é histórico de execução, não índice de plano (`references/01-commits.md`) — um `id` reaparece legitimamente. Exigir unicidade transformaria histórico correto em falha |
| DM-120 | O que conta como prova: o texto `task: T-NN.MM` ou o item inteiro? | **O item inteiro, validado.** `commit` vazio, `null`, marcador de template (`{{...}}`, `TODO`, `NÃO DETERMINADO`) ou identificador malformado **não** prova. Válido é o que o E1 grava: hexadecimal minúsculo de 7 a 40 caracteres, o que `git rev-parse --short HEAD` devolve | Procurar o texto do id aceitaria um item pela metade — exatamente o registro incompleto que a V11 existe para pegar. Não havia parser de `commits` a reutilizar: o leitor do `causa-do-portao.sh` lê chaves de topo escalares e não enxerga lista de mapas |
| DM-121 | A V11 deve confirmar no `git log` que o SHA existe, é ancestral do HEAD e pertence à branch? | **Não.** `ENTREGA.commits` é a evidência canônica do E1 no contrato vigente, e nenhum outro ponto dele exige essas três provas. A V11 fecha a lacuna `tasks.md` ↔ `ENTREGA.commits` e para aí | Ampliar para auditoria de `git log` seria escopo novo sem exigência no contrato — e transformaria uma verificação de registro numa verificação de versionador, com falhas de natureza diferente na mesma linha |
| DM-122 | Qual `causa` uma falha V11 deriva | **Valor novo: `commit_nao_registrado`.** Nenhuma causa existente serve. `tarefa_nao_concluida` afirma que a task não está concluída, e na V11 ela **está** — quem lê iria ao `tasks.md` e não acharia nada errado. `suite_reprovada`, `teste_nao_declarado` e `regressao_nao_declarada` leem campos que a V11 nem toca. `indeterminada` é "não consegui provar", e a V11 **provou**. O valor novo passa as cinco perguntas do DM-111 | Uma causa só para V1 e V11 apagaria a diferença no ponto em que ela é acionável: `tarefa_nao_concluida` manda olhar o status da task; `commit_nao_registrado` manda olhar a lista `commits` do mesmo arquivo |
| DM-123 | Onde a `v11` entra na precedência | **Último do grupo que lê o registro da execução:** `v10`, depois `v6`–`v9`, depois `v1`–`v5` e `v11`. A prova de E1 é a **última** coisa que a execução registra sobre uma task — com a execução interrompida antes (V1), com o `B-NN` aberto (V7) ou com o commit abortado por segredo (V10), a ausência da prova é consequência, não causa | Mesma regra do DM-113, sem mudar nenhuma precedência anterior: `[v1, v11]` → `tarefa_nao_concluida`, `[v7, v11]` → `bloqueio_aberto`, `[v11]` → `commit_nao_registrado` |
| DM-124 | Existe task com `versionado: false` (ou equivalente formal) para isentar a V11? | **Não existe, e não foi inventado.** Os campos obrigatórios de task no contrato de origem são `id`, `status`, `suite`, `arquivos.cria`, `arquivos.altera`, `teste_integracao`, `teste_funcional` e `criterio_aceite` — nenhum `versionado`, nenhum equivalente. `versionado` existe **só** no `kind: entrega`, e é do repositório inteiro. A isenção da V11 é por repositório, nunca por task | Criar a exceção por task exigiria um campo que nenhuma skill grava — e uma isenção que só existe em prosa é uma porta aberta para marcar qualquer task como dispensada da prova |
| DM-125 | ENTREGA histórica com task concluída sem commit | **Duas leituras separadas.** `--validar-historico` continua aceitando snapshot anterior às chaves e **não roda a V11**: nada é reescrito, nenhum commit é inventado, nenhuma causa é inferida. **Entrega ou prontidão nova roda a V11 sempre** — a inconsistência aparece e barra. O conserto é o **E1 tardio**: commita agora, acrescenta o item ao fim de `commits`, sem reordenar o histórico | Silenciar a V11 em entrega nova "por compatibilidade" devolveria exatamente o buraco que a C3 fecha. Reescrever o histórico para fazê-la passar seria maquiar — o oposto do portão |

**O que invalidaria estas decisões:** o contrato passar a exigir, em outro ponto, que o SHA de
`commits` seja provado contra o versionador (aí a V11 herda essa prova, em vez de criá-la); a
sprintx ou a runx passarem a declarar task não versionada com campo tipado (aí o DM-124 ganha o
`n/a` por task que hoje não existe); ou uma verificação futura assumir ordem e sequência de
`commits` — que é C4, e por isso a V11 não as toca.

## P0.2-C4 — `ENTREGA.commits` ganha ordem explícita e durável

O contrato dizia, em três lugares, que os itens de `commits` estão "na ordem em que fecharam" — e
essa ordem existia só na **posição física** da lista. Posição não tem identidade própria: nada
prova que ela não foi trocada, ela não distingue duas passagens da mesma task, não diz numa sessão
nova qual foi o último registro e não dá ao E1 tardio um lugar que signifique "aconteceu agora". A
C3 formalizou o E1 tardio e deixou explícito que ordem e sequência não eram assunto da V11: eram
desta entrega.

| # | Ambiguidade | Decisão tomada | Motivo |
|---|---|---|---|
| DM-126 | Como dar identidade à ordem: data, timestamp do rastro, ou campo próprio | **Campo próprio: `seq`**, inteiro positivo, monotônico e **global à ENTREGA**, gravado em todo item novo. Datas `AAAA-MM-DD` não distinguem dois fechamentos do mesmo dia; o timestamp de `docs/eventos/<trabalho_id>.jsonl` é local da máquina, append-only e **não versionado** — não é fonte canônica. A regra que fica: **datas são registro; quando há ordem contratual, existe campo de ordem** | Ordem contratual precisa ser legível de um arquivo commitado, sem depender de relógio, de fuso, de rastro local nem da memória da sessão. Nenhum dos dois candidatos sobrevive a `git show` numa máquina diferente |
| DM-127 | O que `seq` conta | A **ordem de registro do E1**, e nada mais. **Não** é número da task, número do sprint, rodada da F5, timestamp, nem quantidade de commits Git da branch. Contador **um só por ENTREGA** — nunca por task, nunca por sprint | Um contador por task devolveria o problema: duas tasks diferentes teriam `seq: 1`, e a ordem entre elas voltaria a ser a posição. O alcance global é o que torna o número comparável entre itens |
| DM-128 | Entrega antiga tem itens sem `seq`. Migrar tudo, ou conviver? | **Prefixo legado, sem migração nenhuma.** Itens sem `seq` são aceitos **só no início** da lista, com ordem efetiva pela posição (1..N), e o primeiro item moderno vale **N+1**. Depois do primeiro `seq`, **nenhum item posterior pode omiti-lo**. `[legado, legado, seq 3]` é válida; `[seq 1, legado]` e `[legado, seq 2, legado]` não. **Backfill é proibido**, inclusive "para deixar a lista uniforme" | Reescrever item antigo só para acrescentar chave é a migração em massa que o schema já proíbe (`00-schema.md`, "Regra de migração") — e mexeria em registro de commit que já está no histórico. Prefixo, e não modo, porque a exceção precisa ter fim: aceitar legado a qualquer altura seria não ter a chave |
| DM-129 | Como validar a sequência sem inventar regra para cada defeito | **Uma igualdade só: todo `seq` explícito é igual à posição do seu item** (mais "nenhum legado depois de um moderno"). Como a ordem efetiva de um item **é** a posição dele, essa comparação recusa sozinha as quatro formas de lista quebrada: duplicata, buraco, regressão e reordenação | Quatro verificações independentes divergiriam na primeira manutenção e deixariam combinações fora. Uma invariante que qualquer leitor recalcula de cabeça, e uma bancada que a prova caso a caso |
| DM-130 | O que acontece quando a sequência está quebrada — falha do portão ou violação de contrato? | **Violação de contrato do `ENTREGA.md`: PARA.** Não vira `causa` nova, não vira `indeterminada` e **não entra em `falhas_portao`**. E **nunca se conserta escolhendo outro número** | A mergex é dona da causa **observável do portão** (DM-111), e o portão lê o registro da execução — não a integridade do arquivo que o carrega. Escolher outro número para caber esconderia justamente o registro perdido que a invariante acabou de mostrar |
| DM-131 | Duas sessões podem calcular o mesmo próximo `seq` | **A C4 não resolve corrida, e não finge resolver.** Sem lock, sem seção crítica, sem ownership do índice — isso é da C5. O que a C4 garante é que o resultado de uma corrida (lista com `seq` duplicado) **seja contrato inválido e pare**, em vez de passar despercebido | Meia-solução de concorrência é pior que nenhuma: dá a impressão de exclusão mútua sem tê-la. Tornar a corrida **visível** é o que permite que a C5 a trate depois, e é o que a posição física nunca permitiu |
| DM-132 | Onde o `seq` é calculado, já que o contrato dependia de o agente editar YAML à mão | Num **escritor canônico mínimo**, `scripts/sequencia-de-commits.sh`: lê, valida, calcula o próximo e acrescenta no fim (`--ler`, `--validar`, `--proximo`, `--acrescentar`). Ele **não** reescreve item existente, **não** reordena, **não** faz backfill e mexe só na lista `commits` — `atualizado_em` continua sendo do E1. Não existia escritor executável de `commits`: o E1 mandava editar o YAML | Atribuição de ordem escrita em prosa não é testável nem determinística. Centralizar o cálculo é o mínimo para a bancada e a mutação existirem — e é o ponto onde a C5 poderá pôr a seção crítica, sem que a C4 já a implemente |
| DM-133 | A V11 passa a exigir `seq`, ou a depender de ordem? | **Não, em nenhuma das duas.** A V11 continua perguntando "existe **pelo menos um** commit válido para esta task?" sobre lista totalmente legada, moderna e mista válida; não exige `seq` em leitura histórica, não falha por sequência quebrada e não lê `seq` como quantidade de commits esperada. O que mudou é **de onde ela lê**: o parser de `commits` saiu do `prova-de-commit.sh` e virou o leitor único do `sequencia-de-commits.sh` | Dois parsers para a mesma lista divergem na primeira manutenção — e divergiram: o leitor antigo ancorava o item em `- task:` e ficava **cego** a um item que começa por `seq:`. Um leitor só; a V11 usa dele a leitura crua, porque a ordem não é pergunta dela |
| DM-134 | Onde o E1 tardio entra na série | **Sempre no próximo `seq` global, no fim da lista** — nunca "na posição lógica da task". `seq 1: T-01.01`, `seq 2: T-01.03`, `seq 3: T-01.02` é o registro correto de um E1 tardio | `seq` registra **quando o E1 aconteceu**, não a ordem numérica das tasks. Inserir no meio renumeraria itens já gravados (o que a regra proíbe) e apagaria o fato de que aquele commit só passou a existir depois — exatamente o que a chave existe para preservar |

**O que invalidaria estas decisões:** a C5 introduzir seção crítica para a atribuição do `seq` (o
cálculo continua aqui, mas passa a rodar sob exclusão mútua, e o duplicado deixa de ser desfecho
possível de corrida); um `ENTREGA.md` legado precisar ser reordenado por uma razão externa (aí o
prefixo legado deixa de bastar e a migração vira decisão própria, nunca efeito colateral de uma
gravação); ou uma verificação futura do portão passar a cobrar integridade do registro — que é o
oposto do DM-130 e precisaria de contrato próprio.

## P0.2-C5 — o índice é do E1, e o E1 não é reentrante

A C4 deu identidade à ordem de registro e fez a lista provar a própria integridade: uma corrida
entre dois E1 deixa de passar despercebida e vira contrato inválido. Faltava impedir que a corrida
aconteça. Duas execuções do E1 na mesma worktree compartilham índice, HEAD, `ENTREGA.commits` e o
próximo `seq` — e nenhum desses recursos é da task: todos são da worktree.

| # | Ambiguidade | Decisão tomada | Motivo |
|---|---|---|---|
| DM-135 | Qual é o recurso travado: o repositório, a branch, a ENTREGA ou o índice? | **O ÍNDICE DA WORKTREE.** A trava é do índice daquela árvore de trabalho e de mais nada. Worktrees diferentes têm índices independentes e **fecham tasks em paralelo**; trava no diretório Git comum é proibida | O estado corrompido nasce de dois `git add` no mesmo índice. Travar o repositório inteiro serializaria a sprintx, que abre cada feature em `git worktree` próprio — o custo seria real e o ganho, nenhum: os índices já não se tocam |
| DM-136 | `flock`, arquivo de lock com `O_EXCL`, ou diretório? | **`mkdir` atômico de um diretório.** A criação de diretório falha, atomicamente, quando ele já existe — em Linux, macOS, Windows e Git Bash, que são os ambientes suportados. `flock` não existe nos três | Uma primitiva que só funciona em parte das máquinas transforma exclusão mútua em sorte. O `mkdir` é a única disponível em todas sem dependência nova, e os contratos vivos já se limitam a bash 3.2 e utilitários POSIX |
| DM-137 | Onde a trava mora, já que `.git` pode ser ARQUIVO numa worktree vinculada | **`<git-path do index>.mergex-e1.lock`**, com o caminho vindo de `git rev-parse --git-path index`. Nunca `[ -d .git ]`, nunca caminho montado à mão | O versionador é quem sabe a diferença entre `.git/index` e `<comum>/worktrees/<nome>/index`; qualquer heurística erraria justamente no caso da sprintx. Colada ao índice, a trava é a mesma para todo processo daquela worktree e **outra** para cada worktree, sem nenhuma tabela a manter. E fica em área não versionada por construção |
| DM-138 | Stage já preenchido na entrada: aproveitar, limpar, ou parar? | **PARAR**, sem tocar em nada — nem `reset`, nem `restore --staged`, nem `stash`, nem commit do que se encontrou. O relatório mostra os caminhos staged e o stage fica byte a byte como estava | Não existe prova durável de que este E1 preparou aquele stage: "parece ser da task atual" é palpite sobre trabalho de outra pessoa ou de outro processo. Limpar seria descartar alteração alheia — proibido em qualquer etapa (AGENTS, "Segurança de versionamento") —, e commitar seria amontoar duas tasks. Parar é a única opção que não destrói nada |
| DM-139 | Até onde vai a seção crítica | **Do staging à validação da lista**: A staging, B diff em stage, C verificações do E1, D `git commit`, E captura do SHA, F `--acrescentar`, G validação final. A trava é liberada **só depois de G** | O `seq` é calculado dentro do `--acrescentar` (F). Encerrar a seção antes disso devolveria exatamente a corrida que ela existe para impedir: dois E1 leriam a mesma lista e chegariam ao mesmo próximo número. A validação em G, ainda sob a trava, é a guarda de integridade no ato |
| DM-140 | A varredura de segredo é julgamento do agente e lê `git diff --cached`. Ela fica dentro ou fora da seção? | **Dentro**, e por isso a seção abre em dois tempos: `--preparar` (A a C) deixa a trava adquirida e imprime o token; `--concluir` (D a G) exige esse token e libera. `--fechar` é os dois num processo só, para automação e bancada | O diff em stage só existe depois do staging, que já é a seção crítica. Pôr a varredura fora dela significaria abrir o índice entre a montagem do stage e o commit. Os dois tempos são **uma** sequência com um ponto de suspensão, não um segundo mecanismo: as mesmas funções, na mesma ordem |
| DM-141 | Trava órfã: expirar por idade, por PID vivo, ou nunca? | **Nunca automaticamente.** Trava existente PARA, sem espera e sem remoção. `--status` diagnostica e não altera nada; remover é decisão humana | PID é reutilizado, o ambiente pode ser outro e o stage pode estar preparado pela metade. "A trava parece velha" é heurística, e uma heurística errada aqui remove a proteção justamente quando um E1 está no meio do commit. Recuperação automática de queda não é requisito desta entrega; fingir que é seria pior que não ter |
| DM-142 | Quem pode liberar a trava | **Só o dono**, pelo token que a aquisição imprimiu. `--liberar` com outro token não remove nada e falha | Sem essa exigência, a liberação de um E1 que chegou atrasado apagaria a trava do E1 que está commitando — o mesmo estado que a trava existe para impedir, agora com a aparência de estar protegido |
| DM-143 | Falha depois do staging: a trava fica presa para preservar o estado? | **Não.** Saída normal e falha conhecida liberam sempre; o que **não** acontece é limpeza destrutiva — o stage e o commit parcial ficam onde estão, para diagnóstico | Quem protege o estado dali em diante é a regra do stage de entrada (DM-138): o próximo E1 encontra o índice sujo e para, com relatório. Deixar a trava presa exigiria intervenção manual em toda falha e treinaria a pessoa a removê-la — que é o oposto do DM-141 |
| DM-144 | O commit aconteceu e o append em `ENTREGA.commits` falhou | **PARA e relata "commit Git existe; registro E1 não foi concluído".** Nenhum segundo commit, nenhum retry, nenhum rollback do commit criado | O commit é real e está no histórico: criar outro amontoaria tasks e reverter reescreveria histórico — as duas coisas são proibidas. A V11 (C3) existe exatamente para detectar task concluída sem prova de E1 registrada, e o conserto previsto é o E1 tardio (DM-134), depois que a causa do append estiver resolvida |
| DM-145 | O rastro `docs/eventos/<trabalho_id>.jsonl` pode servir de mutex ou de registro de posse? | **Não.** Ele pode registrar `e1_iniciado`, `e1_concluido` e `e1_recusado_por_lock`, e nada disso decide nada. A verdade da exclusão mútua é a trava do índice | O rastro é local da máquina, append-only e **não versionado** — o mesmo motivo que o desqualificou como fonte da ordem no DM-126. Escrever num arquivo de log não é operação atômica de exclusão, e dois processos podem escrever "comecei" sem que nenhum tenha ganho nada |
| DM-146 | `--acrescentar` precisa de uma segunda trava dentro dele? | **Não, e ele não ganhou nenhuma.** O contrato é que **gravação nova do E1 só acontece sob a seção crítica**, e quem chama o escritor é o executável da seção. A validação de contrato passou a cobrar mecanicamente os call sites executáveis dele. **Leitura nunca trava**: `--ler`, `--validar` e `--proximo` funcionam com a seção ocupada | Uma trava dentro do escritor protegeria o append e não protegeria o par `git add`/`git commit`, que é onde a mistura nasce — daria a impressão de exclusão sem tê-la, que é o erro que o DM-131 recusou na C4. E exigir trava para ler transformaria a V11 e o portão em operações que podem ficar bloqueadas por um E1 em curso |

**O que invalidaria estas decisões:** um ambiente suportado onde `mkdir` de diretório existente não
seja atômico (aí a primitiva muda, e o resto do contrato fica de pé); o C7 trazer o
`ownership-da-task.sh` da C1 e passar a existir prova durável de quem preparou o stage (aí o
DM-138 ganha um caso de "stage reconhecido", que hoje não tem como existir); ou a recuperação
automática de queda virar requisito — o que precisaria de contrato próprio, porque é exatamente o
oposto do DM-141.

## P0.2-C1 — arquivo de task irmã é defeito de plano, não desvio

O contrato do E1 tinha três respostas para um arquivo que mudou: é da task, não mudou, ou é
desvio. Faltava a quarta, e ela apareceu no piloto: **mudou, não é da task que está fechando,
mas outra task da feature a declara**. Sem ela, o mesmo arquivo recebia três respostas
diferentes na mesma máquina — o `escopo-da-task.sh` perguntava pelo escopo unitário, a V9
perguntava pela união, e o `commit-por-task` chamava o caso de mistura de tasks, cujo conselho
("commite uma de cada vez") fecharia de novo uma task congelada.

Esta seção veio da branch histórica `p0.2/mergex-e1-defeito-plano` (commit `c6f1510`), integrada
à linha C3→C4→C5 na P0.2-C7-A. As decisões DM-117 a DM-123 daquela branch colidiam em número com
as da P0.2-C3 (que também partiu de DM-117): renumeradas aqui para DM-147 a DM-153, a próxima
faixa livre depois da P0.2-C5. A branch histórica permanece intacta com a numeração original; a
renumeração vale só para este documento integrado.

| # | Ambiguidade | Decisão tomada | Motivo |
|---|---|---|---|
| DM-147 | Como a mergex nomeia a condição, sem invadir o vocabulário da sprintx | A mergex emite **`arquivo_de_task_irma`** — o que ela **observa**. Ela **não** emite `defeito_de_plano`, que é a **classe** que a sprintx deriva depois. A mergex não grava `00-BLOQUEIOS.md`, não cria `B-NN`, não replaneja e não altera estado da sprintx | É o mesmo corte de dono da DM-111: a mergex é dona da observação mecânica, a skill de origem é dona da classe de pendência. Emitir a classe aqui obrigaria a mergex a conhecer o fluxo `defeito_de_plano → replanejar_execucao → F3/F4/F5 → F6`, que é da sprintx |
| DM-148 | Que interface usar para entregar a condição | A que o repositório **já tem**: um **script determinístico da skill** com **enum fechado**, saída **TSV** e **código de saída** — `scripts/ownership-da-task.sh`, no mesmo molde de `causa-do-portao.sh` e `classificar-atencao.sh`. `0` segue, `2` é a condição (a convenção de bloqueio dos hooks), `1` é "não determinei", `64` é uso. **Nenhum JSON novo, nenhum arquivo novo, nenhum protocolo novo** | O `ENTREGA.md` não serve: a condição acontece **durante** o E1, antes de existir portão, e acrescentar chave ao `kind: entrega` mexeria no schema que a buildx lê. O rastro `docs/eventos/` também não: ele é local da máquina e não versionado. O script é a única camada que já é lida por outra skill sem contrato novo |
| DM-149 | Quem decide qual é a task atual | **Quem chama declara**; o script nunca adivinha. Sem `--task`, com task fora do formato, ou com task que o plano não conhece, ele **recusa responder** (`1`) e não classifica nada. No hook, a task atual sai do rodapé **`Task: T-NN.MM`** que o contrato do E1 já exige na mensagem de commit — do `-m` ou do arquivo do `-F`. Dois rodapes diferentes, ou nenhum, é "não determinada" | Mesmo princípio da DM-108: reconhecimento por prosa não é testável. E é o que mata a mutação "escolher owner pela primeira task encontrada" — sem dono declarado, não há classificação nenhuma |
| DM-150 | Precedência quando o arquivo está na atual **e** numa irmã | É da **atual**, e entra: a interseção com a task que fecha vem antes de qualquer outra regra. As duas tasks ficam registradas na evidência, sem mudar a situação | Ownership unitário significa que a task que fecha é dona do que ela declarou. Mandar para a irmã um arquivo que a atual declarou barraria trabalho legítimo |
| DM-151 | Onde a condição barra, sem promover hook nenhum | No **`commit-por-task`**, e ela é a **única** condição desse hook que **falha fechada mesmo em modo `aviso`**. As demais verificações dele (task não concluída, suíte, mistura) continuam exatamente no modo configurado, e `desligado` continua desligando o hook inteiro. **Nenhum outro hook foi promovido**; a promoção do `escopo-da-task.sh`, que pega na hora da edição, é da sprintx | Deixar passar em aviso produz o dano que o hook existe para impedir — o commit parcial enganoso da task —, e commit no histórico não tem volta. Promover o hook inteiro traria falso positivo nas outras verificações, e hook que atrapalha é desinstalado |
| DM-152 | O que fazer com a V9 | **Nada.** A V9 continua perguntando "este arquivo foi planejado nesta feature?" e continua usando a **união** das tasks. Um `arquivo_de_task_irma` **passa na V9** e **não entra em `desvios`**. A diferença ficou escrita nos dois references | São perguntas diferentes: a V9 existe para pegar o que **ninguém** planejou. Convertê-la para o conjunto unitário faria toda task que toca arquivo de outra reprovar a entrega inteira, inclusive depois de um replanejamento legítimo |
| DM-153 | Plano legado, em que o ownership unitário não é determinável | **Falha fechado no que a mergex controla, sem inventar**: o script recusa responder e não classifica; o E1 não commita. No hook, sem rodapé `Task:` determinável, **vale o comportamento anterior** (a verificação de mistura, no modo configurado) — nenhuma classificação nova é inferida e nenhum falso positivo é criado. **Nenhum plano histórico é migrado** | Inferir o dono pela prosa é exatamente o que este contrato proíbe. E endurecer o caso indeterminável transformaria todo commit sem rodapé — inclusive os `chore(entrega)` de artefato de método — em bloqueio, que é a promoção que o C1 não pode fazer |

**O caso do piloto.** `T-03.01` fechada declarando `tests/ui/cabecalho-topo.test.tsx`; `T-04.03`
em andamento, sem declarar o arquivo; para cumprir a `T-04.03`, o arquivo muda. A mergex
classifica `arquivo_de_task_irma`, não chama de desvio, não o põe no commit da `T-04.03`, não o
apaga nem o restaura, e para o fechamento. Depois de a sprintx replanejar e a `T-04.03` passar a
declarar o arquivo como `altera`, o E1 aceita normalmente — e a `T-03.01` permanece congelada.

**O que invalidaria estas decisões:** o contrato do E1 deixar de exigir o rodapé `Task:` na
mensagem de commit (o hook perderia a única declaração mecânica do dono); a sprintx passar a
gravar a task corrente em artefato tipado e versionado (seria uma fonte melhor que o rodapé, e a
DM-149 mudaria); ou a V9 ganhar um conjunto unitário próprio — o que exigiria rever a DM-152 e a
verificação inteira, e não cabe numa entrega que não abre verificação nova no portão.

## P0.2-C7-B / M1 — ownership contextual e falha fechada

A integração C7-A tornou o ownership parte do E1 serial, mas ainda deixava o classificador procurar
todo `tasks.md` sob `docs/`. Como ids de task se repetem entre trabalhos, o mesmo `T-01.01` histórico
podia atribuir ao trabalho atual um arquivo que ele nunca declarou. M1 fecha a fonte do contexto e
torna os footers prova assertiva antes do staging.

| # | Ambiguidade | Decisão tomada | Motivo |
|---|---|---|---|
| DM-154 | Qual plano participa do ownership | **Somente o plano do trabalho corrente.** O E1 lê `expx_tool` e `trabalho_id` da `ENTREGA.md` explícita; o hook defensivo casa a branch ativa com exatamente uma entrega. SprintX resolve `docs/sprintx/features/<trabalho_id>/` (legado do mesmo trabalho apenas como fallback) e RunX resolve `docs/manutencao/<trabalho_id>/`. Nunca há busca global em `docs/` | Task id não é chave global. Contextualizar pelo trabalho torna feature histórica, ordem alfabética e ordem de criação irrelevantes |
| DM-155 | Ausência de plano significa `n/a`? | **Não.** `ownership=n/a` só existe quando o chamador declara explicitamente a aplicabilidade `n/a`; não é valor novo de `ENTREGA.expx_tool`. No schema vivo, SprintX/RunX são task-based: plano corrente ausente ou ilegível é erro de contrato e para | Falta acidental de artefato não pode reduzir a proteção. Sem informação suficiente, a resposta correta é não determinar, nunca inventar heurística |
| DM-156 | O classificador pode faltar numa instalação task-based | **Não.** `ownership-da-task.sh` ausente é instalação MergeX incompleta e o E1 para antes do staging; doubles task-based instalam o bundle completo | Fail-open por instalação parcial reintroduzia silenciosamente o commit sem ownership justamente onde o contrato o exige |
| DM-157 | Quem seleciona a task e qual papel têm os footers | No E1, **`--task` seleciona**. A mensagem precisa ter exatamente um `Task:` igual ao argumento e um `Trabalho:` igual à entrega. A estrutura é validada antes do staging e o commit produzido é conferido de novo. No hook manual, `Task:` continua sendo a declaração defensiva disponível; rastro nunca seleciona task | Se rodapé selecionasse, argumento e prova poderiam divergir. Separar comando e evidência elimina a ambiguidade sem criar estado novo |
| DM-158 | `desvio` pode ser excluído do commit e o restante ser commitado | **Não.** No caminho normativo, qualquer `desvio` para o fechamento inteiro antes do primeiro `git add`; nenhuma parte da task vira commit parcial. A alteração permanece na árvore, sem restore, stash ou descarte | Um commit “verde” que omite trabalho real da execução é uma prova enganosa e irreversível. O lifecycle posterior decide o encaminhamento |
| DM-159 | Stage preexistente pode ser contextualizado pelo ownership | **Não. DM-138 permanece soberana.** A trava é adquirida, o stage de entrada é conferido e, se não vazio, o E1 para antes de contexto, footers e ownership | Ownership responde pelo arquivo no plano; não prova quem montou o índice. Misturar as duas posses apagaria o limite que C5 tornou mecânico |
| DM-160 | O token de `--preparar` permite concluir com outra entrega/task | **Não.** A seção preparada registra origem, trabalho e task; `--concluir` exige o mesmo contexto e reclassifica o stage contra esse plano antes do commit | O token prova o dono do índice, não o dono lógico dos arquivos. Sem o vínculo, dois trabalhos com `T-01.01` repetido permitiriam preparar em A e registrar/commitar como B |

**O que invalidaria estas decisões:** `ENTREGA.md` deixar de declarar origem/trabalho; SprintX ou
RunX passarem a versionar um ponteiro canônico mais forte para a pasta do plano; ou o E1 deixar de
receber `--task` explicitamente. Nenhuma dessas mudanças autoriza voltar à busca global.

## P0.2-C7-B / M2 — lifecycle executável e recuperação do registro E1

M1 tornou explícitos o trabalho corrente, o ownership e a gramática do E1, mas os artefatos do
método ainda podiam ficar somente na árvore de trabalho, e uma queda depois do commit de produto
não tinha recuperação executável sem criar outro commit. M2 fecha essas duas lacunas sem misturar
produto, lifecycle e seleção de contexto.

| # | Ambiguidade | Decisão tomada | Motivo |
|---|---|---|---|
| DM-161 | O trabalho corrente pode ser escolhido pela branch | **Não.** Argumentos/contexto explícito, conferidos contra `ENTREGA.trabalho_id` e `ENTREGA.origem`, selecionam o trabalho. A branch serve apenas como prova de consistência e nunca como seletor | Branch é transporte, não identidade normativa. Usá-la para selecionar regrediria a fonte contextual fechada pelo M1 e voltaria a permitir colisão entre trabalhos com nomes parecidos |
| DM-162 | Commit de método é um E1 sem task | **Não: é classe de primeira ordem.** E1 tem exatamente um `Task:` e um `Trabalho:`, sem `Metodo:`. Método tem exatamente um `Trabalho:` e um `Metodo:` no enum `pre-e2\|pre-e6\|e8`, sem `Task:`. Híbrido, duplicata e valor fora do enum são inválidos. Commit de método não entra em `ENTREGA.commits`, não consome `seq` e não satisfaz V11 | As duas classes provam eventos diferentes. Reusar parcialmente a gramática do E1 faria o hook rejeitar lifecycle legítimo ou, pior, deixaria um checkpoint contar como produto de uma task |
| DM-163 | E2 e E6 podem persistir seus próprios artefatos ao entrar | **Não.** `pre-e2` e `pre-e6` são checkpoints explícitos, e E2/E6 têm barreiras executáveis que apenas confirmam que o checkpoint já foi cumprido. E8, último escritor, persiste o terminal no próprio checkpoint `e8` | Conserto silencioso apaga a fronteira auditável entre produzir artefatos e validá-los. E8 é a exceção necessária porque não há etapa posterior que possa persistir seu estado terminal |
| DM-164 | O catálogo cumulativo permite staging amplo | **Não.** Cumulativo significa conjunto elegível fechado do trabalho corrente: somente paths previstos pelo contrato, existentes e realmente dirty. Em SprintX, `00-PLANEJAMENTO.md` é elegível quando o fluxo corrente o usa; não é inventado quando ausente. `base/**` só entra pelos arquivos enumerados no índice/contrato, e `HISTORICO.md` só pelo path global exato previsto pelo trabalho. Nunca `git add -A`, `git add docs/` ou busca global | O checkpoint deve persistir todo o método elegível sem absorver produto, outro trabalho ou arquivo histórico por coincidência de nome |
| DM-165 | Retomada pode depender de memória local da sessão | **Não.** A idempotência vem do Git, dos trailers e do estado versionado: sem mudança elegível, o checkpoint é `NO-OP` e não cria commit vazio; se o mesmo estado já foi persistido antes da queda, a nova chamada o reconhece e não duplica o commit | Rastro local desaparece entre máquinas e sessões. A prova que sobrevive à queda é a que já está no histórico ou no artefato versionado |
| DM-166 | Lifecycle pode usar outro lock ou aceitar stage existente | **Não.** Checkpoints usam a mesma trava C5, exigem stage vazio na entrada, montam somente o catálogo fechado e passam pela mesma varredura de segredo. Nenhum arquivo de produto é aceito no commit de método | Índice, HEAD e staging continuam sendo recursos únicos da worktree. Uma segunda regra de posse reabriria exatamente a corrida que C5 fechou |
| DM-167 | Como registrar um commit E1 que já existe quando só o append falhou | Pela ação explícita `--registrar-existente`, sob a trava C5 e com stage vazio. Ela exige SHA de 40 hex existente, alcançável de `HEAD`, não-merge, exatamente um `Task:` e um `Trabalho:` correspondentes, nenhum `Metodo:` e ownership válido sobre todos os paths do commit — origem e destino em rename. Não cria, amenda, reseta, rebasa nem aplica commit | O objeto já existente é a prova de produto. Validá-lo e acrescentar a referência recupera o registro perdido sem fabricar um segundo E1 nem reescrever história |
| DM-168 | Quais são a idempotência e os conflitos da recuperação | Mesmo SHA e mesma task já registrados são `NO-OP`, sem novo item e sem novo `seq`; o mesmo SHA em outra task é conflito; outro SHA para a mesma task é permitido e recebe o próximo `seq`, porque a lista admite histórico múltiplo. Depois do append, `ENTREGA.md` fica dirty e somente o próximo `pre-e2` a versiona | A recuperação corrige apenas a prova em `ENTREGA.commits`. Versionar a entrega dentro dela misturaria de novo E1 e lifecycle, enquanto proibir múltiplos SHAs por task contrariaria o histórico já admitido pela C4 |

**O que invalidaria estas decisões:** `ENTREGA.md` deixar de identificar trabalho e origem; o Git
deixar de ser a fonte durável do checkpoint; ou surgir uma classe nova de commit com trailers
próprios, que exigiria ampliar a gramática fechada. Fixture E8/HISTORICO, `.gitattributes`, política
de CRLF e runbook de lock órfão permanecem deliberadamente para M3; nenhuma decisão acima os
antecipa. SprintX e BuildX não são alteradas por este marco.

## P0.2-C7-B / M3 — fixture histórica, lock órfão e portabilidade

| # | Ambiguidade | Decisão tomada | Motivo |
|---|---|---|---|
| DM-169 | O checkout pode materializar scripts shell em CRLF quando `core.autocrlf=true` | **Não.** Todo `.sh` versionado usa `text eol=lf`; clone Windows e worktree vinculada precisam provar `i/lf`, `w/lf`, atributo `eol=lf`, ausência byte a byte de CR e shebang literal | Um blob LF não basta para execução portável: sem atributo, o checkout Windows pode reescrever o arquivo e quebrar o interpretador antes de qualquer teste rodar |
| DM-170 | A máquina pode decidir que uma trava C5 é órfã e removê-la | **Não.** Recuperação é humana, parte de `--status`, prova worktree/índice/dono e stage vazio, remove somente o caminho literal examinado e revalida `estado=livre`; não existe `--force-unlock`, expiração por idade ou heurística de PID | PID pode ser reutilizado, relógio não prova posse e o stage pode estar preparado pela metade. Remoção automática recriaria a corrida que a trava existe para impedir |

**O que invalidaria estas decisões:** o suporte a scripts shell ou a checkout Windows deixar de
existir; ou surgir um protocolo durável, atômico e multiplataforma capaz de provar abandono e posse
do stage sem julgamento humano. Até lá, LF e recuperação conservadora são requisitos de contrato.
